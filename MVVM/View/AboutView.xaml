<UserControl x:Class="Gotyeam.MVVM.View.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="Transparent">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Acerca de" Foreground="White" FontSize="24" FontWeight="Bold"/>
            <TextBlock Text="Información sobre la aplicación y el autor" Foreground="LightGray" FontSize="14" Margin="0,5,0,0"/>
        </StackPanel>

        <Border Grid.Row="1" CornerRadius="10" Padding="30" HorizontalAlignment="Left" Width="600" VerticalAlignment="Top">
            <Border.Background>
                <SolidColorBrush Color="{StaticResource secondaryBackColor1}"/>
            </Border.Background>
            <StackPanel>
                <TextBlock Text="Gotyeam - Gestor de Videojuegos" Foreground="White" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,10"/>
                
                <!-- Aquí el usuario puede poner su texto personalizado -->
                <TextBlock TextWrapping="Wrap" Foreground="LightGray" FontSize="15" Margin="0,0,0,20" LineHeight="22">
                    <Run Text="Desarrollado por: "/><Run Text="Haoyi Li, Marco Antonio Achig y Adriano Zender" FontWeight="Bold" Foreground="{StaticResource color1}"/><LineBreak/><LineBreak/>
                    <Run Text="Esta aplicación ha sido creada como parte del Proyecto final de la asignatura de Desarrollo de Interfaces."/><LineBreak/>
                    <Run Text="Su propósito principal es servir como una plataforma integral para la gestión de usuarios, empleados y una extensa base de datos de videojuegos."/>
                </TextBlock>

                <TextBlock Text="Tecnologías utilizadas:" Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="• C# .NET" Foreground="LightGray" FontSize="14" Width="150"/>
                    <TextBlock Text="• WPF (Windows Presentation Foundation)" Foreground="LightGray" FontSize="14"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="• Arquitectura MVVM" Foreground="LightGray" FontSize="14" Width="150"/>
                    <TextBlock Text="• FontAwesome.WPF" Foreground="LightGray" FontSize="14"/>
                </StackPanel>

                <TextBlock Text="Versión 1.0.0" Foreground="#888888" FontSize="12" Margin="0,30,0,0" HorizontalAlignment="Right"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
