using System;

namespace Gotyeam.MVVM.ViewModel
{
    public class AboutViewModel : ViewModelBase
    {
        // View model for the About section.
        // Currently empty as the content will be static in the view, 
        // but could hold dynamic properties if needed.
        public AboutViewModel()
        {
        }
    }
}
